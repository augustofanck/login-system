{
  "name": "login-system-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"mvnw.cmd spring-boot:run\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}